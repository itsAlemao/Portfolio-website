# Portfolio-website
Personal portfolio website, built with **ASP.NET** developed using **Blazor** framework and **MudBlazor** framework extension 

# How to run it
After cloning the repo run the command `dotnet run`
